// Firebase Configuration
// This file is auto-imported by @react-native-firebase/app
// The actual configuration comes from google-services.json (Android) and GoogleService-Info.plist (iOS)

export const firebaseConfig = {
    // Configuration is handled natively by Firebase SDK
    // You don't need to manually specify config here for React Native
};

export default firebaseConfig;
